user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    # Edge rate limiting by client IP: coarse anti-flood protection that shields the backend before requests reach it.
    # Precise per-user limits (20 req / 10s) are enforced in the app via Redis, since Nginx cannot read the user from the JWT.
    limit_req_zone $binary_remote_addr zone=bookmark_api:10m rate=10r/s;
    limit_req_status 429;

    upstream bookmark_backend {
        server bookmark-service:8080;
    }

    upstream portal_frontend {
        server bookmark-portal:3000;
    }

    server {
        listen 80;
        server_name _;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;


        location /api/bookmark_service/swagger/ {
            proxy_pass http://bookmark_backend/swagger/;
        }

        location /api/bookmark_service/ {
            limit_req zone=bookmark_api burst=20 nodelay;
            proxy_pass http://bookmark_backend/api/bookmark_service/;
        }

        location ~ /\. {
            deny all;
        }

        location / {
            proxy_pass http://portal_frontend;
        }
    }
}